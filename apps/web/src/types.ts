export type Session = { user: { id: string; fullName: string; email: string }; workspace: Workspace };
export type Workspace = { id: string; name: string; profession: string; defaultCurrency: string; brandColor: string; whatsappNumber?: string; paymentInstructions?: string; bankDetails: Record<string,string>; foreignAccountDetails: Record<string,string>; paymentLinks: Array<{label:string;url:string}>; logoFileId?:string };
export type Client = { id:string; name:string; email?:string; phone?:string; companyName?:string; notes?:string };
export type Proposal = { id:string; title:string; status:string; clientId:string; currency:string; totalAmount:string; depositAmount?:string; depositPercent?:string; paymentDueDate?:string; currentVersion?:{contentJson:ProposalContent}; client?:Client };
export type ProposalContent = { problemSummary:string; proposedSolution:string; deliverables:string[]; timeline:string; lineItems:Array<{description:string;quantity:number;rate:number}>; paymentTerms:string; callToAction:string };
export type Invoice = { id:string;title:string;amount:string;currency:string;dueDate?:string;status:string;instructions:string;proposalId:string;proposal?:{title:string;totalAmount:string};client?:Client;confirmations?:Array<{id:string;clientName:string;paymentMethod:string;amountPaid:string;transactionReference:string;note?:string;receiptFileId?:string;submittedAt:string}> };
